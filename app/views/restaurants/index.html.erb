<div class="container">
  <div class="d-flex flex-column align-items-center vh-100 mt-5">
    <h1 class="mb-3">Restaurants</h1>
    <p class="mb-3"><%= link_to "Create a new Restaurant", new_restaurant_path %></p>
    <ul>
    <% @restaurants.each do |restaurant| %>
      <li class="mb-2"><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
    <% end %>
    </ul>
  </div>
</div>
